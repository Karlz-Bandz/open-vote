services:
  postgres:
    image: postgres:16
    container_name: open-vote-postgres
    environment:
      POSTGRES_DB: open-vote
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5430:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: